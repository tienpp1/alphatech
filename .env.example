# ==============================================================================
# AI Business Operations Platform - Environment Configuration Template
# ==============================================================================

# Django Core
DEBUG=True
SECRET_KEY=django-insecure-change-me-in-production-ai-business-platform-2026
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Configuration (PostgreSQL + PostGIS)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=ai_business_platform_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=127.0.0.1
DB_PORT=5432

# Optional GDAL/GEOS paths for Windows environment if not in system PATH
# GDAL_LIBRARY_PATH=C:/Program Files/PostgreSQL/18/bin/libgdal-35.dll
# GEOS_LIBRARY_PATH=C:/Program Files/PostgreSQL/18/bin/libgeos_c.dll

# AI & LLM Configuration (Phase 8+)
LLM_PROVIDER=gemini
LLM_API_KEY=
LLM_MODEL=gemini-2.5-flash
EMBEDDING_MODEL=text-embedding-004

# Application Defaults
DEFAULT_PAGE_SIZE=20
WORKSPACE_DEFAULT=retail
